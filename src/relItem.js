class RelItem {
  // has: id, name, category, rating, number_of_reviews, price(which may be a range so shouldbe a string here!)
  static all = [];
  static parContainer = document.GetElementById('relatedItemCarousel')


}